# Core FastA# Core FastAPI
fastapi>=0.104.0,<0.115.0
uvicorn[standard]>=0.24.0,<0.32.0
pydantic>=2.5.0,<3.0.0
python-multipart>=0.0.6
python-dotenv>=1.0.0
httpx>=0.25.0
aiofiles>=23.0.0

# Document Processing
docstrange

# ML/AI - Core
# NOTE: torch, torchvision, torchaudio installed separately in Dockerfile with CUDA 12.1 support
# Do not add them here to avoid dependency conflicts
transformers>=4.36.0,<4.50.0
# Upgraded for better NumPy 2.x support
accelerate>=0.28.0
# BitsAndBytes 0.43.0+ for NumPy 2.x + RTX 4090 Ada Lovelace architecture
bitsandbytes>=0.43.0

# ML/AI - Supporting
safetensors>=0.4.3
sentencepiece>=0.1.99
protobuf>=4.25.0,<6.0.0
huggingface-hub>=0.23.0

# Scientific Stack - NumPy 2.x compatible versions
# scipy 1.13.0+ supports NumPy 2.x (fixes sph_legendre_p ufunc error)
scipy>=1.13.0
# pandas 2.2.0+ supports NumPy 2.x
pandas>=2.2.0

# Image Processing - CRITICAL for NumPy 2.x compatibility
# opencv-python 4.10.0+ required for NumPy 2.x (major NumPy consumer)
opencv-python>=4.10.0
# scikit-image 0.24.0+ required for NumPy 2.x (uses scipy internally)
scikit-image>=0.24.0
# Pillow 10.3.0+ for proper NumPy 2.x support
pillow>=10.3.0

# Utilities
requests>=2.31.0
# NumPy 2.x - all dependencies now support it
numpy>=2.0.0,<3.0.0

# Build dependencies (needed for compilation)
packaging>=23.0
wheel>=0.40.0
setuptools>=65.0

# Flash Attention 2 installed separately in Dockerfile (optional optimization)
# flash-attn>=2.5.0

# RunPod
runpod>=1.3.0